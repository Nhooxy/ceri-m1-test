## Customize the test machine
machine:
  timezone:
    Europe/Paris

  java:
    version: oraclejdk8

## Customize checkout
#checkout:
#  post:
#    - git submodule sync
#    - git submodule update --init # use submodules

## Customize deployment commands
deployment:
  staging:
    branch: master
    heroku:
          appname: Pokedex-Pierre